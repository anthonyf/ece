## 1. Implementation

- [x] 1.1 Add `ece-boolean-p` wrapper function for `boolean?`
- [x] 1.2 Add all new primitives to `*primitive-procedure-names*` and `*primitive-procedure-objects*` lists
- [x] 1.3 Export new symbols: `number?`, `string?`, `symbol?`, `boolean?`, `zero?`, `eq?`, `equal?`, `modulo`, `even?`, `odd?`, `positive?`, `negative?`

## 2. Tests

- [x] 2.1 Add tests for type predicates (`number?`, `string?`, `symbol?`, `boolean?`, `zero?`)
- [x] 2.2 Add tests for equality primitives (`eq?`, `equal?`)
- [x] 2.3 Add tests for numeric utilities (`modulo`, `abs`, `min`, `max`, `even?`, `odd?`, `positive?`, `negative?`)
