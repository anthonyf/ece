## 1. Implementation

- [x] 1.1 Add `letrec` macro definition (expands to `let` + `set`)

## 2. Tests

- [x] 2.1 Add tests: single recursive binding, mutual recursion, tail position
- [x] 2.2 Run all tests and verify pass
