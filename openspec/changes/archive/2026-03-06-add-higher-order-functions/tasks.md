## 1. Core Implementation

- [x] 1.1 Define `filter` as an ECE function via `evaluate` (same pattern as `map`)
- [x] 1.2 Define `reduce` as an ECE function via `evaluate`
- [x] 1.3 Define `for-each` as an ECE function via `evaluate`

## 2. Tests

- [x] 2.1 Add tests for `filter` (even?, no matches, empty list, lambda predicate)
- [x] 2.2 Add tests for `reduce` (sum, empty list, cons-reverse)
- [x] 2.3 Add tests for `for-each` (returns nil)
- [x] 2.4 Run tests and verify all pass
