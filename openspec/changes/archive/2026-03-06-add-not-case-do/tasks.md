## 1. Implement case macro

- [x] 1.1 Add `case` macro definition (expanding to lambda application + cond)
- [x] 1.2 Export `case` from the ECE package

## 2. Implement do macro

- [x] 2.1 Add `do` macro definition (expanding to named let)
- [x] 2.2 Export `do` from the ECE package

## 3. Tests

- [x] 3.1 Add tests for `case` (single datum, datum list, else, no match, key eval once, symbols)
- [x] 3.2 Add tests for `do` (counting, accumulating, body side effects, no step, immediate termination)
- [x] 3.3 Run all tests and verify pass
