## 1. Scripts

- [x] 1.1 Copy `scripts/cl-indent.el` from dunge-cl
- [x] 1.2 Create `scripts/scheme-indent.el` using Scheme mode
- [x] 1.3 Create `scripts/pre-commit` hook script

## 2. Makefile

- [x] 2.1 Create `Makefile` with test, repl, fmt, check-fmt, setup, and clean targets

## 3. Verify

- [x] 3.1 Run `make setup` and confirm hook is symlinked
- [x] 3.2 Run `make fmt` and confirm files are formatted
- [x] 3.3 Run `make test` and confirm tests pass
