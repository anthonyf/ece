## 1. Implement define-record macro

- [x] 1.1 Add `define-record` macro to the stdlib section of `src/ece.lisp` that generates constructor, predicate, accessors, mutators, functional update accessors, and copy function using hash tables as the underlying store

## 2. Tests

- [x] 2.1 Add tests for `define-record`: constructor, predicate, accessors, mutators, functional update, copy, hash table compatibility, and multiple record types
