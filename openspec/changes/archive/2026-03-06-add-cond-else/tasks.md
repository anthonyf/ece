## 1. Implementation

- [x] 1.1 Modify `cond` macro to check for `else` as clause test, expanding to just `(begin body...)` instead of conditional

## 2. Tests

- [x] 2.1 Add tests for `else` clause and `t` clause as catch-all
- [x] 2.2 Run all tests and verify pass
