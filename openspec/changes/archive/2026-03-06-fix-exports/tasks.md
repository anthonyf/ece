## 1. Export missing symbols

- [x] 1.1 Add `display`, `newline`, `null?`, `eof?`, `primitive` to the `:export` list in the `ece` package definition

## 2. Clean up tests

- [x] 2.1 Replace all `ece::` prefixed symbols in tests with unqualified symbols where the symbol is exported
