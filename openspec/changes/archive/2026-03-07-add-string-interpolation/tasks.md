## 1. String Interpolation Reader

- [x] 1.1 Add custom string reader to `*ece-readtable*` in `src/ece.lisp` that handles `$var`, `$(expr)`, and `$$`
- [x] 1.2 Add tests for string interpolation (variable, expression, literal `$$`, plain strings)

## 2. Lines Function

- [x] 2.1 Add `lines` function to `src/prelude.scm`
- [x] 2.2 Add tests for `lines` (multiple args, single arg, empty, mixed types)

## 3. Verify

- [x] 3.1 Run full test suite and confirm all existing tests pass
